node_modules
dist
.env
*.log

# SQLite database - contains real household data, not source
server/prisma/dev.db
server/prisma/dev.db-journal

# Test database - recreated fresh on every test run
server/prisma/test.db
server/prisma/test.db-journal

# Ad-hoc DB backups taken before risky changes - real household data, not source
server/prisma/backups/
